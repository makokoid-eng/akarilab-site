# implementation-notes ── akarilab-site

仕様書に書かれていなかった判断・変更・妥協・意思決定の記録（decision-log スキル準拠）。

2026-08-05 16:59 ── [works/index.html ほか（8本目デモ「三崎社会保険労務士事務所」追加）]
- 事実：仕様書のデモURLが「【ここにURLを入れる】」のプレースホルダのままだった。
- 判断：`https://sharoushi-demo.vercel.app/` を採用した。
- 理由：ローカルの `C:/Users/user/akarilab-demos/sharoushi-demo/README.md` に想定URLとして明記されており、実際にアクセスして HTTP 200・タイトル「トップ｜三崎社会保険労務士事務所（デモサイト）」を確認できたため。
- 代替案：ユーザーに質問して確定を待つ／プレースホルダのまま実装。前者は自律実行中でブロックする、後者はデプロイ時にリンク切れになるため不採用。
- 影響/未解決：なし（公開確認済み）。

2026-08-05 16:59 ── [works/index.html（セクション番号の繰り下げ）]
- 事実：士業セクションを「06」として挿入し、既存の「制作のご相談」を 06→07 に繰り下げた。
- 判断：業種セクション（01〜06）の後に相談セクションが来る既存の並びを維持した。
- 理由：番号は表示上の連番であり、士業をデモ群の末尾（追加順）に置くのが既存パターン（医療＝あかり内科が最後に追加され05）と一致するため。
- 代替案：士業を 07 にして相談を 06 のまま残す → 業種→相談の流れが崩れるため不採用。
- 影響/未解決：なし。

2026-08-05 16:59 ── [works/index.html（meta description / og:description への「士業」追記）]
- 事実：仕様書の指示は「件数表記の更新」だったが、業種を列挙している meta description と og:description に「士業」（と og 側に欠けていた「医療」）も追記した。
- 判断：件数だけでなく業種リストも実態に合わせた。
- 理由：業種の列挙が実ページ内容と食い違うのは SEO 上の不整合であり、追記のリスクはほぼゼロのため。
- 代替案：件数表記のみ直す → リストの欠落が残るため不採用。
- 影響/未解決：description が2文字ほど伸びたが、検索結果での切り詰め許容範囲内。

2026-08-05 16:59 ── [llms.txt / sitemap.xml（周辺ファイルの同期）]
- 事実：仕様書に明示されていないが、llms.txt の「7デモ」→「8デモ」（士業追記）と、sitemap.xml の `/` と `/works/` の lastmod を 2026-08-05 に更新した。
- 判断：件数表記の grep 横断修正の一環として llms.txt を対象に含め、変更ページの lastmod も合わせて更新した。
- 理由：llms.txt は「7デモ」という件数表記そのもの（仕様の grep 指示の範囲内）。lastmod 更新はこのリポの既存運用（aiseo-page-publisher の同期対象）に合わせたため。
- 代替案：sitemap は触らない → 変更検知が遅れるだけで害はないが、既存運用に反するため不採用。
- 影響/未解決：なし。

2026-08-05 16:59 ── [サムネイル画像]
- 事実：/works/ のカードは個別サムネイル画像を持たない設計だった（画像は OGP 用の works/img/seizo-btob.jpg が1枚あるのみ）。
- 判断：8本目にも画像は追加せず、ユーザー側の画像準備作業も「不要」と報告する。
- 理由：既存7本と同じ見た目に揃えるという仕様に対し、画像なしが既存の形式のため。
- 代替案：なし。
- 影響/未解決：将来カードにサムネイルを導入する場合は全8枚一括で設計する。

2026-09-07 13:20 ── [GA4タグの網羅（23ページへ追加）]
- 事実：GA4プロパティ akarilab.org（測定ID末尾 0YYP）はデータを受信していたが、リポジトリ内 166 HTML のうち 25 HTML に gtag.js が入っていなかった。抜けていたのは /kokora/ /hidamari/ /moyalog/ /repimemo/ と /makoto/services/** の全サービスページ、および grant-portal と attendance-ocr の thanks（決済後の着地）。本番でも実測し、/kokora/ が gtag.js を1件も読み込まないことを確認した。
- 判断：assets/partials/head-common.html と一字一句同じスニペットを、23ページの <head> 内 viewport 直後に挿入した。
- 理由：抜けていたのが「見る→申し込む」の導線そのもので、CV に一番近い thanks ページまで未計測だった。head-common.html を include して生成したページには入るが、手書きページには誰も貼らないという構造の穴で、放置すると増え続ける。
- 代替案：全ページを build_pages.py の生成対象に寄せる → 影響範囲が大きく、今回の目的（計測の穴を塞ぐ）に対して過剰。将来やるなら別作業として切る。
- 影響/未解決：hidamari.html（/hidamari/ への転送スタブ）と owner/**（本人専用の運用ポータル）は意図的に除外した。前者は実体のない page_view が増えるため、後者は自分の訪問を数えないため。

2026-09-07 13:20 ── [scripts/check_ga4_tag.py と CI への組み込み]
- 事実：上記の欠落は誰も気づかないまま残っていた。
- 判断：タグの欠落・二重・測定IDの取り違え・</head> より後ろへの混入を fail で止めるスクリプトを追加し、aiseo-check.yml の fail 系に入れた。
- 理由：人の注意力に依存させると必ずまた抜ける。
- 代替案：warning 止まりにする → 今回まさに「気づかない」ことが問題だったので fail にした。
- 影響/未解決：実装途中、<meta http-equiv="refresh"> を持つページを一律スキップしかけた。/r/** の51ページは「転送前に redirect_click を送る」ことが目的のページで、スキップすると計測の要が検査対象から外れる。スクリプトのコメントに理由を残して自動スキップは実装しない方針にした。

2026-09-07 13:20 ── [privacy-policy.html（GA4とクッキーの記載を実態に合わせた）]
- 事実：第8条が「本サービスはLINE上で提供されるため、ウェブサイトにおけるクッキーの利用は最小限に留めます。将来的にWebサービスを提供する場合は、別途クッキーポリシーを定めます」と書いていた。実際には akarilab.org は GA4 のクッキー（_ga）で計測稼働中で、第4条の第三者提供にも Google LLC の記載が無かった。計測しているのに書いていない状態。
- 判断：第4条に「4.4 アクセス解析事業者への送信」を新設（旧4.4 法令に基づく開示は4.5へ繰り下げ）、第8条を書き換え、冒頭の適用範囲に「当ウェブサイトのアクセス解析とクッキー」を明記、最終更新日を2026-09-07に更新した。
- 理由：過去に「プライバシーポリシーと実装が矛盾していた」事故があり、同じ形の矛盾を放置できない。オプトアウト手段（アドオン・ブラウザ設定）も明示した。
- 代替案：GA4を止めてポリシーを据え置く → 計測を捨てることになるため不採用。
- 影響/未解決：このポリシーは全体が「ひだまり」向けの文面のまま。サイト全体・ここら・勤怠OCR・助成金ポータルを1本で受けるには本来は構成から見直しが要る。今回はGA4の矛盾だけを潰した。構成の見直しは未着手。

2026-09-07 13:20 ── [キーイベント未設定への対応（スクリプトのみ／未実行）]
- 事実：GA4のキーイベントは既定の purchase 1件のみで、毎日飛んでいる redirect_click がキーイベントになっていなかった。GA4上は「キーイベント 0」のまま。
- 判断：scripts/setup_ga4_key_events.py と .github/workflows/setup-ga4-key-events.yml を追加した。既存の setup_ga4_dimensions.py と同じサービスアカウント方式。dry-run が既定で、--apply / workflow の apply=true で登録する。
- 理由：GA4の設定変更は本人の権限で行うべきで、AIが勝手に実行しない。手順を書くだけでは実行時に迷うので、実行可能な形にして渡す。
- 代替案：GA4のUIで手作業 → 手順は残るが再現性が無いため、スクリプトを正とした。
- 影響/未解決：**未実行**。キーイベントは登録時点以降のデータにしか適用されない（さかのぼらない）ので、実行が遅れるほど取り逃す。
