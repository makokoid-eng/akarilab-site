# implementation-notes（仕様外の判断・変更の記録）

2026-07-07 00:10 Stripe直接決済（page_stripe_payment_design.md）実装中の仕様外判断2件：
1. scripts/build_redirects.py の ALLOWED_CATEGORIES に "stripe" を追加。設計mdは /r/stripe-consult/ 新設のみ想定していたが、カテゴリのバリデーションで弾かれたため。redirect-monitor 側は category を集計軸にしているだけなので影響なしと判断（推測。次回の週次レポートで stripe カテゴリが出力されるか要確認）。
2. tokushoho.html への追記は、設計mdの「決済手段の一文追記」ではなく「相談・受託サービスに関する表記」の独立節＋導入文への案内文を追加。既存ページの冒頭が「ひだまり」専用の表記と宣言しており、一文追記だと適用範囲が矛盾するため。

2026-07-10 10:45 redirect-metrics.yml に TLS証明書期限チェックを追加（ユーザー依頼。demo.akarilab.org 証明書発行停止事故の再発検知が目的）。仕様外判断3件：
1. 実装場所は collect_redirect_metrics.py 内ではなく workflow の独立ステップ（bash+openssl）にした。GA4集計と関心が別で、Python側の失敗と分離できるため。
2. 警告閾値は残り14日未満。GitHub Pages / Let's Encrypt は通常30日前に自動更新するので、14日を切っていれば「自動更新が壊れている」と判断できる（推測含む。実際の更新タイミングは今後の週次レポートで観察）。
3. 対象ドメインは akarilab.org / demo.akarilab.org の2つ。他ブランド（hidamari等）はパス運用でドメインを持たないため対象外。新ドメイン追加時は workflow の for ループに足す。

2026-07-10 現在時刻不明（検証修正エージェント） makoto/services/index.html カード7（Brain AI実用例20選）の価格矛盾を解決。検証指摘が2件で相互矛盾（指摘1「仕様上980円が正、可視テキスト4,980円を直せ」vs 指摘2「JSON-LD の "980" は "4980" からの桁落ち誤編集、戻せ」）。リポジトリ内の証拠（data/sales_products.yml standard_price:4980、index.html:749、makoto/profile/index.html 2箇所、docs/aiseo/page_services_design.md:147 すべて4,980円）から、指摘2の桁落ち診断が正しいと判断し、JSON-LD offers.price を "4980" に復元、可視テキスト「4,980円（買い切り）」は維持した。タスク仕様文中の「7 Brain 980円」も同じ桁落ちの伝播と解釈（推測）。実売価格が本当に980円へ変更されていた場合は逆修正が必要。

2026-07-16 index.html に「04 Works」セクション（制作実績デモ2件）を追加（ユーザー依頼。ハブ&スポーク一方通行の導線設計が目的）。仕様外判断3件：
1. 配置は Services(03) の直後。案件の提案用デモであり自社プロダクトではないため、Products(01) ではなく受託文脈に隣接させた。結果 Updates / About は 05 / 06 に繰り下げ。
2. 新規CSSは追加せず、既存の .services（lead用）+ .product-list + .product-card を再利用。デモ追加時は product-card を1枚足すだけで済む構造にした。JSON-LD への ItemList 追加は見送り（デモは架空データで検索面に載せる価値がないと判断。推測を含む）。
3. 各デモ側から akarilab.org への発リンク（<a href>）は皆無のため、デモ側の修正は行っていない。掲載する側のデモにはフッターにテキストの制作クレジットとヒーロー画像への焼き込みで「AkariLab」表記があるが、いずれもリンクではなく、ハブから公開リンクを張る以上の露出を増やさないため許容（当初仕様どおり「テキストのクレジットは可・リンクは不可」）。掲載しない側のデモには制作者を示す表記が一切ない（実測）。今後デモに制作者クレジットを入れる場合もリンクは張らない方針。
   ※検証手法の注意：この環境の Git Bash の grep は「複数の -e ＋ --exclude-dir」でクラッシュ（exit 134）し、`| head` を挟むと無出力＝ヒット0件に見える。当初この罠で「クレジットなし」と誤判定した。デモ側の走査は Python 等で行うこと。画像に焼き込まれた文字は grep では原理的に検出できない。

2026-07-16 Works セクションは公開時点でデモ1件（架空データの不動産デモ）のみ掲載。もう1件の受託デモは掲載を見送った（ユーザー判断）。方針：そのデモ自体は顧客提案のため一般公開を継続するが、AkariLab とは現時点で紐づけない。「公開してよいか」と「AkariLab の実績として結びつけるか」を別問題として扱う判断。理由の要旨は、架空データではなく実在クライアントの個別情報を含む提案モックであり、先方の同意なく AkariLab の実績欄から誘導する段階にないため。掲載条件（架空データ化 or 掲載許諾）と対象の特定情報は、本リポジトリが public のためここには書かない。ローカルの非公開メモ（~/.claude 配下の memory）に記録済み。

2026-07-16 akarilab.myshopify.com を index.html の Products セクションに「AkariLab STORE」カードとして追加（ユーザー依頼「これも繋いで」）。仕様外判断4件：
1. 配置は Works ではなく Products。これは受託デモ（一宮・沖縄・dog）ではなく AkariLab 自身のストア（live title「AkariLabデモストア」、/password へリダイレクト、商品ページ0件、「まもなく公開」）。ユーザーに Works4枚目/別扱い/別セクションの3択で確認し「別扱い（Products等）」を選択。受託デモではないためブランド分離の対象外で、ドメインに akarilab を含み本体と紐づくのは自然。
2. リンクは張らない（ユーザー判断）。当初「繋いで」を a タグで実装したが、パスワード保護中でクリックすると /password 画面に飛ぶため、既存の未公開プロダクト（りぴメモ=開発中・店舗運営支援Bot=社内運用中）と同じリンクなしの div カードに統一。ストア公開時に a タグへ戻す手順を index.html の当該コメントに明記。
3. パスワード保護中のため中身は非公開。meta に「まもなく公開」を明記し、リンクなしで行き止まりを作らない。
4. 逆行リンク：myshopify 側の live HTML に akarilab.org へのリンク・文字列は0件（curl で確認）。外部リンクは Shopify 公式と /admin のみ。一方通行は保たれている。
